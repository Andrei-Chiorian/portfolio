import styled from "styled-components";

export const StylesHome = styled.div`
    .home-section {
        width: 100%;
        height: auto;
        clear: both;
        float: left;
    }
    section {
        display: block;
        unicode-bidi: isolate;
    }
`;
